#include <stdio.h>
#include <conio.h>
#include <stdlib.h>


int main()
{
    /*
    int a;
    scanf("%d", &a);

    // забираю \n из потока
    char b;
    scanf("%с", &b);

    char str[10];
    gets(str);
    puts(str);
    */
    for(int i =0; i<100 ;i++)
        printf("%4d\n", i);
    return 0;
}

